import './styles.css';

const ButtonIcon = () => {
    return (
        <button className='btn btn-primary btn-icon'>
            <p>Ver Catálogo</p>
        </button>
    );
};

export default ButtonIcon;